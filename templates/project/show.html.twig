{% extends 'base.html.twig' %}

{% block title %}Hello ProjectShow!{% endblock %}

{% block body %}
<h1>{{ project.title }}</h1>
<p>{{ project.description }}</p>

<h2>Ajouter un Commentaire</h2>
{{ form(commentForm) }}

{% for comment in comments %}
    <p>{{ comment.name }}</p>
    <p>{{ comment.description }}</p>
{% endfor %}

<a href="{{ path('app_project') }}">Retourner à la liste des projets</a>
{% endblock %}